apiVersion: v1
kind: Service
metadata:
  name: ai-platform-api
  namespace: ai-platform
spec:
  selector:
    app: ai-platform-api

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: ClusterIP
